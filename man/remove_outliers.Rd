% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Remove_Outliers.R
\name{remove_outliers}
\alias{remove_outliers}
\title{Zastępuje wartości odstające `NA` w jednej lub wielu kolumnach}
\usage{
remove_outliers(df, cols, method = c("SD", "IQR"))
}
\arguments{
\item{df}{Ramka danych zawierająca kolumny do oczyszczenia.}

\item{cols}{Wektor nazw kolumn numerycznych jako ciągi znaków.}

\item{method}{Metoda wykrywania outlierów: `"SD"` (domyślnie 3 sigma)
lub `"IQR"` (1.5 * IQR). Wybór jest dokonywany automatycznie poprzez `match.arg`.}
}
\value{
Ramka danych z wartościami odstającymi zastąpionymi `NA`.
}
\description{
Funkcja identyfikuje wartości odstające (outliers) w podanych kolumnach
numerycznych i zastępuje je `NA`. Pozwala zachować wszystkie wiersze ramki danych,
co jest przydatne w analizie szeregów czasowych.
}
\examples{
df <- data.frame(x = c(1,2,3,100,4,5), y = c(10,12,15,200,14,13))
remove_outliers(df, c("x","y"), method = "SD")
remove_outliers(df, c("x","y"), method = "IQR")
}
